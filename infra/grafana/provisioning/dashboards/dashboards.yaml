apiVersion: 1

providers:
  - name: ClusterOps
    orgId: 1
    folder: ClusterOps
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
